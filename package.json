{
  "name": "hapi-namespace",
  "version": "0.0.1",
  "description": "Add namespace prefixes to Hapi routes, like express-namespace",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "hapi",
    "namespace",
    "routing",
    "prefix"
  ],
  "author": "dsernst",
  "license": "ISC",
  "devDependencies": {
    "should": "^7.1.1"
  }
}
